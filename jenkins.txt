pipeline {
  agent none
    stages {
      stage ('BUILD') {
      agent {label 'flower'}
    steps {
    echo "this is build"
    }
}
stage ('DEPLOY') {
 agent {label 'flower'}
  steps {
    echo "this is deploy"
    }
}
}
}
